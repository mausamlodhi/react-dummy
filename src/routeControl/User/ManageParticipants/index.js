import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  MANAGE_PARTICIPANTS: {
    path: `${baseRoutes.userBaseRoutes}manage-participants`,
  },
};

export default accessRoute;
