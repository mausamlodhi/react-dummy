import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  ACTIVITY: {
    path: `${baseRoutes.userBaseRoutes}activity`,
  },
  ACTIVITY_BLANK: {
    path: `${baseRoutes.userBaseRoutes}activity-blank`,
  },
};

export default accessRoute;
