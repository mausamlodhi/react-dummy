import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  LOOPS: {
    path: `${baseRoutes.userBaseRoutes}loops`,
  },
  LOOPS_BLANK: {
    path: `${baseRoutes.userBaseRoutes}loops-blank`,
  },
};

export default accessRoute;
